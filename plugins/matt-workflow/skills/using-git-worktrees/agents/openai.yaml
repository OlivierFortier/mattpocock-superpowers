interface:
  display_name: "Using Git Worktrees"
  short_description: "Create or reuse an isolated Git worktree"
  default_prompt: "Use $using-git-worktrees to perform this subflow."
policy:
  allow_implicit_invocation: false
