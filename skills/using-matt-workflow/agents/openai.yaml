interface:
  display_name: "Matt Workflow"
  short_description: "Route software work through Matt skills"
  default_prompt: "Use $using-matt-workflow to route this work and state the next gate."
policy:
  allow_implicit_invocation: true
